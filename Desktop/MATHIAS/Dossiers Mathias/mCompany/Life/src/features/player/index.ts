/**
 * @module features/player
 * Player feature barrel — locomotion, combat, parkour state machines.
 *
 * Import via: import { PlayerController } from '@features/player'
 */

export { PlayerController } from '../../gameplay/PlayerController';
export { CombatSystem } from '../../gameplay/CombatSystem';
